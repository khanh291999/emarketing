@model IEnumerable<WebApplication1.Models.Rateviewmodel>
@{
    ViewBag.Title = "ViewRate";
    Layout = "~/Views/Shared/_Layout1.cshtml";
}
<style>
    td {
        padding: 20px;
    }
    #name {
        color: #3498db;
        font-weight: bold;
        text-transform: uppercase;
    }

        #name:hover {
            text-decoration: underline;
        }
    .center {
        text-align: center;
        margin-bottom: 20px;
    }

        .center h1 {
            font-weight: bold;
            color: red
        }
</style>


<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>View Rate</title>
    <link href="~/Content/paginition.css" rel="stylesheet" />
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" />
    <link href="~/Content/bootstrap.css" rel="stylesheet" />
</head>
<body>
    <div class="container" style="height:1150px; width:60%; margin-top:2% ">

        <div class="center"><h1>Rating</h1></div>
        @if (Model != null)
        {
            <table class="table table-striped">

                <tr>
                    <th>Username</th>
                    <th>Rate</th>
                </tr>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td id="name">@item.u_name</td>
                            <td>@item.rate_content</td>
                        </tr>
                    }
                </tbody>
            </table>
        }
    </div>







</body>
</html>