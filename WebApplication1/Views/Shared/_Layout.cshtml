
<!DOCTYPE html>
<html>
<head>
    <style>
        .na {
            text-decoration: none;
            color: white;
        }
        .navbar1 {
            position: relative;
            min-height: 50px;
            border: 1px solid transparent
        }
    </style>
    <title>@Page.Title</title>
    @RenderSection("head", required: false)

    <link href="~/Content/bootstrap.css" rel="stylesheet" />
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" />
</head>
<body>
    <nav class="navbar1 navbar-inverse" style="">
        <div class="container-fluid">
            @*<div class="navbar-header">
    
            </div>*@
            <ul class="nav navbar-nav">
                <li>@Html.ActionLink("Home", "Index", "Home", null, new { @class = "ht" })</li>
                <li>@Html.ActionLink("Category", "Index", "User", null, new { @class = "ht" })</li>
                <li>
                    @if (Session["u_id"] != null)
                    {
                        @Html.ActionLink("Post An Ad", "Login", "User", null, new { @class = "ht" })

                    }

                </li>
                <li>
                    @if (Session["ad_id"] != null)
                    {
                        @Html.ActionLink("Edit Category", "login", "Admin", null, new { @class = "ht" })

                    }
                </li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                @*<li>
                    @if (Session["u_id"] == null)
                    {

                        @Html.ActionLink("Sign In", "login", "User", null, new { @class = "ht", })

                    }
                </li>*@
                <li>
                    @if (Session["u_id"] != null)
                    {
                        @Html.ActionLink("Sign Out", "Signout", "User", null, new { @class = "ht" })
                    }
                

<li>
                <li>
                    @if (Session["ad_id"] == null)
                    {
                        @Html.ActionLink("Admin", "login", "Admin", null, new { @class = "ht" })
                    }

                <li>
            </ul>
        </div>
        @*@using (@Html.BeginForm())
            {
                <div class="navbar-form navbar-left">
                    <div class="form-group">

                        <input type="text" class="form-control" placeholder="Search" name="search">
                    </div>
                    <button type="submit" class="btn btn-success">Search</button>
                </div>
            }*@
    </nav>

    @RenderBody()
    <script src="~/Scripts/bootstrap.min.js"></script>
    <script src="~/Scripts/bootstrap.js"></script>
</body>
</html>
